using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class Rectangle{

    int width, hieght;
    Vector2 direction, position;
    public Rectangle(int x, int y, int width, int hieght, Vector2 direction)
    {
        position = new Vector2(x, y);
        this.width = width;
        this.hieght = hieght;
        this.direction = direction;
    }
    public void changePosition(int x, int y)
    {
        position.x = x;
        position.y = y;
    }
    public void changeSize(int width, int hieght)
    {
        this.width = width;
        this.hieght = hieght;
    }
    public List<Tile> getTileCollisions()
    {
        List<Tile> tiles = new List<Tile>();
        for (int x = (int)position.x; x < (int)position.x+width; x++)
        {
            for (int y = (int)hieght; y < (int)position.y+hieght; y++)
            {
                tiles.Add(Map.Instance.getTile(x, y));
            }
        }
        return tiles;
    }
}
*/