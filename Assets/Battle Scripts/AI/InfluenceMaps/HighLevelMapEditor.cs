using UnityEngine;
using UnityEditor;
using System.Collections;
using UnityEngine.UIElements;
//Reference for editor scrips, leave here
/*
namespace InfluenceMap
{
    [CustomEditor(typeof(HighLevelMap))]
    public class HighLevelMapEditor : Editor
    {
        public override void OnInspectorGUI()
        {
            HighLevelMap map = (HighLevelMap)target;
            if (GUILayout.Button("Initialise path"))
                map.InitPathind();
            if(GUILayout.Button("Update path"))
                map.UpdatePathind();
            base.OnInspectorGUI();
        }
    }
}*/